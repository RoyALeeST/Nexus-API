{
  "configurations": [
    {
      "type": "node-terminal",
      "name": "NestJS: Debug",
      "request": "launch",
      "command": "npm run start:dev",
      "cwd": "${workspaceFolder}"
    }
  ]
}
